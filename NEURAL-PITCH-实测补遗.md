# NEURAL-PITCH 实测补遗（2026-09-20）

> 这份文件是 `NEURAL-PITCH.md` 的实测对照：原文档的"最小验证路径"已经跑完，
> 另外补做了原文没覆盖的对照实验。**结论与原文有两处相反，请注意。**
> 所有数字都是本机（i9-14900HX / RTX 5060 Laptop / CPU 推理）实测，脚本见文末。

## 结论速览

1. **方案 A（神经分轨）对"伴奏抢线"无效** —— 三个模型（含 SOTA BS-RoFormer SDR 12.98）分轨后，
   目标高音仍然检不到。分离能让**有声率**提升（+19 个百分点），但解决不了"跟谁"。
2. **方案 B（神经音高，RMVPE）值得做，但不要把 GPU 算进去** —— RMVPE 在 CPU 上
   **比现有 pYIN 还快**（142 分钟素材 54 秒），DirectML 反而慢 8.6 倍。
   实测漏音事件数减少 **2~3 倍**、中断总时长减少 **60~70%**。
3. **"伴奏抢线"这个此前认为无解的问题，RMVPE 能解** —— 在用户指认的 41:10–41:12
   （2470–2472s），pYIN 与手写 YIN 都报 115–263Hz，**RMVPE 报 521→597Hz（C5→D5）、
   置信 0.6–0.9**，谱证据显示高音基波与低音源强度相当（71.5 vs 60.0）。
   这是继"漏音少 2~3 倍"之后，接入 RMVPE 的第二个、也是更重要的理由。

---

## 一、方案 A 实测（"最小验证路径"）

对象：`lovecan陶喆.m4a` 41:14 附近（20.4s 片段，绝对时刻 2465–2485s）。
方法：分轨 → 对人声轨跑现有 pYIN → 看 41:14 的高音 D5（587Hz）能否被检出。

| 模型 | 人声 SDR | 人声轨在 D5 周期的 CMNDF | 输出 |
|---|---|---|---|
| htdemucs | 8.5 | 1.54 / 0.88 / 1.31 / 1.47 / 1.21 / 0.23 | 313/238/233/277/349/558Hz，D5 未出现 |
| mdx_extra | 9.0 | 1.58 / 0.95 / 0.98 / 1.46 / 1.16 / 0.23 | 同上 |
| BS-RoFormer ep317 | **12.98** | 1.56 / 1.28 / 1.34 / 1.45 / 1.11 / 0.22 | 同上 |

**机制**（不是 SDR 不够）：人声轨与混音在 500–700Hz **逐谱峰相同**——模型把 D5 的邻居分音
（622/635/657Hz）也判给人声，而这些非谐波邻居恰好抹掉 D5 的周期性。文献亦明确
"完全/部分重叠的音源无法分离"。

**副产物（有用的）**：分离对**有声率**确有帮助（该片段 74.1% → 92.9%），
所以如果目标是"减少漏音"，分离有效；目标是"跟对人声"，分离无效。

**低成本闸门（建议以后评估任何前处理都先跑）**：理想隔离测试——只保留目标谐波串
（587Hz 的 1~5 次，±1.2%）再跑 pYIN：CMNDF 从 1.1~1.7 降到 **0.008~0.027**，
pYIN 立刻以 0.85 概率报出 D5。**这一步几分钟就能判定"某条前处理路线是否可能有救"。**

---

## 二、方案 B 实测（RMVPE，人声专用音高模型）

工具：`rmvpe-onnx`（纯 ONNX 封装，MIT；模型来自 HF `lj1995/VoiceConversionWebUI`，MIT）。

### 2.1 定标检验（合成正弦）

| 输入 | RMVPE 输出 | 偏差 |
|---|---|---|
| 220 Hz | 220.2 Hz | +2 音分 |
| 440 Hz | 439.7 Hz | −1 音分 |
| 587 Hz | 587.6 Hz | +2 音分 |
| 880 Hz | 878.7 Hz | −3 音分 |
| 110 Hz | 112.6 Hz | +40 音分（低端弱区） |

### 2.2 速度（20.4s 音频）

| 后端 | 耗时 | 实时率 RTF |
|---|---|---|
| **CPU** | **0.24 s** | **0.012** |
| DirectML (RTX 5060) | 2.06 s | 0.101 |

DirectML 慢 **8.6 倍**，且 ORT 警告有算子回退到 CPU。**结论：音高模型走纯 CPU，
DirectML/CUDA EP 只增加复杂度。**（原文"需要 GPU"只对**分离**成立：
BS-RoFormer RTF 22、htdemucs RTF 2~3。）

### 2.3 A/B 对照（5 个真实素材，同一套指标 + 同一套显示后处理）

| 素材 | 算法 | 有声率 | 中位Hz | >600Hz | 漏音事件/总时长 | CPU 实时率 |
|---|---|---|---|---|---|---|
| bili_cd_test (224s) | pYIN | 71.6% | 249 | 1.4% | 46 个 / 43.6s | 0.02 |
| | **RMVPE** | **95.9%** | 272 | 5.1% | **2 个 / 1.4s** | 0.049 |
| feijichang (286s) | pYIN | 68.9% | 168 | 0.0% | 49 个 / 44.9s | 0.02 |
| | **RMVPE** | **95.5%** | 273 | 0.5% | **4 个 / 2.2s** | 0.040 |
| wangchunfeng (179s) | pYIN | 88.0% | 224 | 0.0% | 10 个 / 4.5s | 0.02 |
| | **RMVPE** | **96.3%** | 234 | 0.0% | **3 个 / 0.9s** | 0.069 |
| lovecan (142min) | pYIN | 76.7% | 211 | 1.9% | 988 个 / 1031s | 0.03 |
| | **RMVPE** | **90.2%** | 244 | 2.4% | **478 个 / 407s** | **0.006** |

（漏音定义：输出中断 ≥0.28s，且中断两侧同为有声、音高相差 ≤3 半音；两者都做了同样的
"0.28s 保持 + 锚点防跳变"显示后处理，帧率各自 43fps / 100fps。）

**帧级一致率 17~49%**，但逐秒对照显示这是**换音/颤音处的抖动**而非"各跟各的源"：

```
feijichang 25–45s            秒   pYIN      RMVPE
秒   pYIN      RMVPE         35   C#3(140)  C#3(138)
25   C#3(137)  C#3(140)      36   C#3(138)  C#3(139)
27   G#3(205)  G#3(208)      38   E3(167)   F#3(183)
28   F#3(186)  F#3(189)      39   A#2(116)  A#3(232)  ← 分歧
33   G3(193)   G3(201)       41   G#3(208)  A3(217)
```

### 2.4 41:10–41:12 那段（用户指认的 D5 位置，绝对时刻 2470–2472s）★

| 判据 | 结果 |
|---|---|
| **RMVPE** | **521→526Hz（C5）→ 597Hz（D5）**，置信 0.60–0.90，D5 带显著性在 2471.4s 达 1.000 |
| pYIN（现状） | 115–263Hz（低八度以上，跟到了伴奏） |
| 手写 YIN 路径 | 2470s C4(262)、2471s A#2(118.8)、2472s A#3(236.7) |
| 谱证据（Goertzel 基波幅度） | 2470.5s：587Hz 基波 **71.5** vs 262Hz 基波 60.0；2471.0s：20.6 vs 14.6；2471.4s：24.5 vs 22.3 |

**结论：这一段就是原文预测的情形——人声在 C5–D5，但周期性强不过伴奏，
所以 pYIN 与手写 YIN 双双被抢线；RMVPE 靠音色特征正确锁住了人声。**
（注：2474s 附近是另一回事，那里连 RMVPE 也给 D5 频段只有 1.3% 的显著性，
即那个瞬间人声确实不占主导；两个时刻不要混为一谈。）

---

## 三、对原文档的修正清单

| 原文说法 | 实测 | 建议 |
|---|---|---|
| "神经网络需要 GPU" | 音高模型 CPU RTF 0.012（比 pYIN 还快）；只有**分离**需要 GPU | 不必升 CUDA/cu130；RTX 5060 对本路线无用 |
| "推荐 ONNX Runtime + DirectML" | DirectML 比 CPU 慢 8.6 倍 + 算子回退 | 用 `Microsoft.ML.OnnxRuntime`（CPU）即可 |
| "模型体积仅加几十 MB" | 运行时确实只几十 MB，但 **RMVPE 模型本体 345MB** | 单文件 147MB → 约 500MB；可量化到约 90MB（需重测精度） |
| 数据集用 MAESTRO / NSynth | 前者钢琴、后者合成单音，验证不了"人声 vs 伴奏" | 用 MIR-1K / MedleyDB / MUSDB18(vocals) / iKala |
| （未提）评估指标 | 我早期用"灵敏参考路径"当判据，把**假声弱基波误判成漏音**，差点改错方向 | 评估必须多判据交叉（频谱 + 独立算法 + 听感） |
| （未提）低成本闸门 | 理想隔离测试几分钟就能判"前处理是否有救" | 任何前处理方案先跑它 |

## 四、接入建议（若决定做）

1. **RMVPE 作为第三种算法**（"YIN / pYIN / RMVPE"），不替换 pYIN；显示层（平滑、锚点、
   外观）全部复用。
2. **ONNX Runtime CPU + 单文件内嵌模型**；先做离线整曲分析，实时模式需要新写滑动窗封装。
3. **待办**：① 声区判定（真声/混声/假声）现在挂在 pYIN 候选上，需改用频谱量移植；
   ② 模型量化与精度重测；③ 4 个素材 + 现场录音的回归对比（本次 A/B 脚本可直接复用）；
   ④ 已知边界的表述要更新：**"伴奏抢线"已由 RMVPE 实测解决**（41:10–41:12 处 C5→D5），
   剩下的边界只是"人声在某瞬间确实被压住"（如 2474s 附近）。
4. **不要做方案 A**。

## 五、本次用到的脚本与环境（都在 `D:\pb_pyin_lab`，不在仓库内）

- `gate_oracle.py` 理想隔离闸门 / `gate_demucs.py` Demucs 分轨 /
  `gate_roformer.py` BS-RoFormer / `gate_rmvpe.py` RMVPE
- `ab_rmvpe.py` RMVPE vs pYIN 完整 A/B（生成上面 2.3 的表）
- `validate_rule.py`、`exp_final.py`、`hi_eval.py`：Python 版 pYIN 复刻与参数扫描工具链
- Python 包：`torch(CPU)`、`demucs`、`bs-roformer-infer`、`rmvpe-onnx`、`torchcrepe`、`onnxruntime(-directml)`
- 模型：`bs_roformer.ckpt` 610MB、`bs_roformer_q.onnx` 158MB（已无用可删）、RMVPE ONNX 345MB
